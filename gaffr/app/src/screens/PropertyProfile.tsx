import React, { Component } from 'react';
import { View, Text, Image, ScrollView, AsyncStorage } from 'react-native';

export default class PropertyProfile extends Component {
  state = {};

  render() {
    return (
      <View>
        <Text>here is the property profile</Text>
      </View>
    );
  }
}
